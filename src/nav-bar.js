import React from 'react'

class NavBar extends React.Component {
	render() {
		console.log(this.props.stateProp)
		return (
			{/*
			< div >
				<h1>Quote it!</h1>
				<span><Link to={{ pathname: '/', state: this.props.stateProp.quoteObj }}>Random Quote(API)</Link></span>
				&emsp; | &emsp;
		<span><Link to='/rndQuoteLocal'>Random Quote(Local)</Link></span>
				&emsp;| &emsp;
		<span><Link to='/addQuote'>Add Quote</Link></span>
				&emsp;| &emsp;
		<span><Link to='/listQuotes'>List all Quotes(Local)</Link></span>
			</div >
		*/}
		)
	}
}

export default NavBar